// Production environment.
// The apiUrl is set to the App Platform component URL of the backend.
// If you attach a custom domain, replace the host below.
export const environment = {
  production: true,
  apiUrl: 'https://stb-backend.ondigitalocean.app/api'
};
